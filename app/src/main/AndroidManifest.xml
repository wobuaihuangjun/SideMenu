<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huangzj.side">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".myslidmenu.CustomSlidActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".example.FirstActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".example.SecondActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".example.ThirdActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".example.FourActivity"
            android:launchMode="singleTask" />

    </application>

</manifest>
